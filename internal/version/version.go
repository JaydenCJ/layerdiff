// Package version pins the single source of truth for the layerdiff
// version string; the CLI, the JSON envelope, and hash manifests all
// quote it.
package version

// Version is the semantic version of this build.
const Version = "0.1.0"
